
on:
  issues:
    types:
      - labeled

jobs:
  test-bukkit:
    if: github.event.label.name == 'solved'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Hello from Octo Organization